{
  "name": "swarm",
  "version": "0.1.0",
  "description": "Cross-terminal agent coordination via Cmux and A2A",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "swarm": "bin/swarm"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --import tsx --test tests/*.test.ts",
    "prebuild": "rm -rf dist"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT",
  "dependencies": {
    "@a2a-js/sdk": "^0.3.13",
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
